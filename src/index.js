//React
import React from 'react';
import ReactDOM from 'react-dom';

//in App
import ListingWidget from './components/app';

ReactDOM.render(
    <ListingWidget/>, document.querySelector('.container')
);
